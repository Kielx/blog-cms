extends layout.pug

block content
  .page
    h2 Create new post form!
    form#addPost(action='/add/addpost', method='post', name='addPost')
      label(for='addPostTitle') Enter post title: 
      br
      input#addPostTitle(type='text', name='title', value='New post title')
      br
      label(for='content') Enter post title: 
      br
      textarea(type='textarea', name="content", cols="30", rows="10" form='addPost') New post content
      input(type='submit', value='Create new post!')
