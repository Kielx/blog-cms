extends layout.pug



block content
    - var param = params.postname
    .page
      each post in posts
        if post['newSlug'] == param
          h2= post.title
          p= post['content']
          hr
          h5= `Added by ${post.author} on ${post.added.toGMTString()}`

          
